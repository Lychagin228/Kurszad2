# Курсовой проект по дисциплине: "Основы программирования и алгоритмизации"

## 📋 Задания к КП

Разработка программы анализа функций

## 📁 Структура проекта

├── .gitattributes

├── .gitignore

├── Kurslab2.vcxproj

├── Kurslab2.vcxproj.filters

├── Kurszad2.slnx

├── README.md

├── Task.c

├── analysis.c

└── analysis.h

## 🔧 Реализованные функции

1. 'double f(double x)'

Назначение: Вычисление значения кусочной функции для заданного аргумента x.

Параметры:

x - аргумент функции, тип double.

Возвращает: значение функции f(x) в точке x, тип double.

Алгоритм: Проверяет условие x < -2, если истинно - вычисляет atan(2*x). Иначе проверяет x < 0, если истинно - вычисляет (e^x - x - 1)/(x*x). Иначе вычисляет sqrt(x*x + 4).

2. 'int menu()'

Назначение: Вывод на экран главного меню программы с перечнем доступных операций.

Параметры: отсутствуют.

Возвращает: 0 в случае успешного выполнения.

Алгоритм: Выводит форматированное меню с шестью пунктами операций.

3. 'double tochka(double x)'

Назначение: Интерфейсная функция для вычисления значения функции в точке (вызывается из меню).

Параметры:

x - аргумент функции, тип double.

Возвращает: значение функции f(x) в точке x, тип double.

Алгоритм: Вызывает функцию f(x) и возвращает ее результат.

4. 'int tablica(double start, double end, double step)'

Назначение: Формирование и вывод таблицы значений функции на заданном интервале.

Параметры:

start - начало интервала, тип double.

end - конец интервала, тип double.

step - шаг табуляции, тип double.

Возвращает: 0 в случае успешного выполнения.

Алгоритм: В цикле от start до end с шагом step вычисляет значение функции для каждого x и выводит пару (x, f(x)) в форматированной таблице.

5. 'double integral(double a, double b, double n)'

Назначение: Приближенное вычисление определенного интеграла методом средних прямоугольников.

Параметры:

a - нижний предел интегрирования, тип double.

b - верхний предел интегрирования, тип double.

n - параметр, обратный количеству разбиений, тип double.

Возвращает: приближенное значение интеграла, тип double.

Алгоритм: Вычисляет шаг h = (b-a)*n, затем в цикле суммирует значения функции в средних точках каждого прямоугольника, окончательный результат - сумма, умноженная на h.

6. 'double poisk_x(double y, double eps)'

Назначение: Поиск аргумента x, при котором значение функции приближенно равно заданному y.

Параметры:

y - целевое значение функции, тип double.

eps - допустимая погрешность, тип double.

Возвращает: найденное значение x или 0.0 если решение не найдено, тип double.

Алгоритм: Перебирает значения x от -10.0 до 10.0 с шагом 0.001, для каждого вычисляет f(x) и проверяет условие |f(x)-y| < eps. При выполнении условия возвращает текущий x.

7. 'double proizvodnaya(double x)'

Назначение: Вычисление приближенного значения производной функции в точке.

Параметры:

x - точка, в которой вычисляется производная, тип double.

Возвращает: приближенное значение производной, тип double.

Алгоритм: Использует метод правой разностной производной: f'(x) ≈ (f(x+h)-f(x))/h, где h=0.000001.




Подготовил Лычагин Антон. бИЦ-252.
